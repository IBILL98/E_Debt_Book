<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_debt_book">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditMarketPhone"></activity>
        <activity android:name=".EditMarketPassword" />
        <activity android:name=".editCustomer" />
        <activity android:name=".addCustomerFromMarket" />
        <activity android:name=".addNewDebt" />
        <activity android:name=".myCustomers" />
        <activity android:name=".MarketMain" />
        <activity android:name=".NumberVerification" />
        <activity android:name=".MarketRegister" />
        <activity android:name=".CustomerRegister" />
        <activity android:name=".CustomerMain" />
        <activity android:name=".EditMarketInfo" />
        <activity android:name=".MarketMain1">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>